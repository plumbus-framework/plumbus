// ── Vitest Config Re-export ──
// Consumer apps import vitest config utilities through the framework.
// Usage: import { defineConfig } from "plumbus-core/vitest";

export { defineConfig } from 'vitest/config';
