// ── Server Module ──
// Fastify server bootstrap: wires up auth, routes, error handling, and middleware.
//
// Key exports: createServer

export { createServer } from './bootstrap.js';
export type { PlumbusServer, ServerConfig } from './bootstrap.js';
