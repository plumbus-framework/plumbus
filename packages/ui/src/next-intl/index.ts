// ── next-intl Re-export ──
// Consumer apps import next-intl through the framework — no separate next-intl dependency needed.
// Usage: import { useTranslations, NextIntlClientProvider } from "@plumbus/ui/next-intl";

export { NextIntlClientProvider, useTranslations } from 'next-intl';
